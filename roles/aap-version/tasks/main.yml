---
# tasks file for aap-version
- name: Validate AAP Token with Version Awareness
  block:
    - name: Probe base AAP API to detect routing paths
      ansible.builtin.uri:
        url: "{{ controller_url }}/api/"
        method: GET
        headers:
          Content-Type: "application/json"
        validate_certs: "{{ validate_aap_certs | default(false) }}"
      register: aap_api_base
      ignore_errors: true

    - name: Debug aap_base
      ansible.builtin.debug:
        var: aap_api_base

    - name: Determine the correct /me endpoint URL based on API structure
      ansible.builtin.set_fact:
        aap_me_endpoint: >-
          {% if aap_api_base.json is defined
             and aap_api_base.json.available_versions is defined
             and 'controller/v2' in aap_api_base.json.available_versions.keys() | string %}
          {{ controller_url }}/api/controller/v2/me/
          {% else %}
          {{ controller_url }}/api/v2/me/
          {% endif %}

    - name: Test AAP token validity against detected endpoint
      ansible.builtin.uri:
        url: "{{ aap_me_endpoint | trim }}"
        method: GET
        headers:
          Authorization: "Bearer {{ aap_token }}"
          Content-Type: "application/json"
        validate_certs: "{{ validate_aap_certs | default(false) }}"
      register: aap_token_test
      ignore_errors: true

    - name: Assert that the AAP token is valid
      ansible.builtin.assert:
        that:
          - aap_token_test.status == 200
          - aap_token_test.json.username is defined
        fail_msg: |
          AAP Token Validation Failed!
          Attempted Endpoint: {{ aap_me_endpoint | trim }}
          HTTP Status Code: {{ aap_token_test.status | default('No Response') }}
          Error Details: {{ aap_token_test.json | default(aap_token_test.msg | default('Could not reach the AAP controller host.')) }}
        success_msg: "AAP token is valid on this platform version. Authenticated as: {{ aap_token_test.json.username }}"

# - name: Query the Controller configuration endpoint
#   ansible.builtin.uri:
#     url: "{{ controller_url }}/api/"
#     method: GET
#     status_code: 200
#     headers:
#       Authorization: "Bearer {{ controller_token }}"
#       Content-Type: "application/json"
#   register: controller_config

# - name: Set aap_version fact
#   ansible.builtin.set_fact:
#     aap_version: "{{ controller_config.x_api_product_version }}"

# - name: Set controller api based on aap_version
#   ansible.builtin.set_fact:
#     controller_api_url: >-
#       {% if aap_version is version('4.5', '>=') %}
#         api/v2
#       {% elif aap_version is version('2.5', '>=') and aap_version is version('4.5', '<') %}
#         api/controller/v2
#       {% endif %}

# - name: Show detected AAP Version
#   ansible.builtin.debug:
#     msg: "Detected AAP Version: {{ aap_version }} | Using API URL: {{ controller_api_url | trim }}"
